{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "installCommand": "apt-get update && apt-get install -y portaudio19-dev && pip install -r requirements.txt",
  "postinstall": "apt-get install -y portaudio19-dev"
}
